import { test } from "@playwright/test";

test.skip("bootstrap e2e scaffold", async () => {
  // Placeholder to keep the Playwright runner wired while real e2e tests
  // arrive in later implementation tasks.
});
