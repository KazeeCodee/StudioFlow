# Compatibilidad para servicios Railway existentes que aun usan Nixpacks.
# Los servicios nuevos se configuran con Railpack en railway*.toml.
[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
